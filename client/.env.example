# Copy this file to `.env.local` or `.env` (do NOT commit real keys)
# Add your Google Maps Embed API key below. Vite exposes env variables prefixed with VITE_.
VITE_GOOGLE_MAPS_API_KEY=your_google_maps_api_key_here

# Example usage: create `client/.env.local` with the real key and restart dev server.